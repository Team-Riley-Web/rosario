---
import rosarioIconGold from '../assets/images/rosario-icon-gold.png';

export interface Props {
  position?: 'fixed' | 'sticky';
  homeInitialMinimal?: boolean;
}

const { homeInitialMinimal = false } = Astro.props;
const scrollThreshold = homeInitialMinimal ? 0 : 60;
const accountHref = `https://${import.meta.env.PUBLIC_SHOPIFY_STORE_DOMAIN || 'your-store.myshopify.com'}/account`;
const navLinks = [
  ['Shop', '/shop'],
  ['About', '/about'],
  ['FAQ', '/faq'],
  ['Contact', '/contact'],
];
const shopDropdownLinks = [
  ['Murano', '/shop?group=murano'],
  ['Pearls', '/shop?group=pearls'],
  ['All', '/shop?group=all'],
];
---

<header
  id="site-nav"
  x-data={`{ open: false, scrolled: false, pastMarquee: false, announcementProgress: 0, navProgress: 0, homeInitialMinimal: ${homeInitialMinimal ? 'true' : 'false'} }`}
  x-init={`
    const hero = document.getElementById('hero');
    const update = () => {
      const y = window.scrollY;
      scrolled = y > ${scrollThreshold} || !hero;
      announcementProgress = hero ? Math.min(y, 44) : 44;
      navProgress = hero ? Math.min(y / 44, 1) : (scrolled ? 1 : 0);
      pastMarquee = announcementProgress >= 44;
      const navHeight = 72 - (navProgress * 16);
      const announcementHeight = homeInitialMinimal && hero ? 44 - announcementProgress : 0;
      document.documentElement.style.setProperty('--site-nav-offset', (navHeight + announcementHeight) + 'px');
    };
    window.addEventListener('scroll', update, { passive: true });
    update();
  `}
  :class="{ 'scrolled': scrolled }"
  class:list={['site-nav', { 'home-initial-minimal': homeInitialMinimal }]}
  role="banner"
  @keydown.escape.window="open = false; document.body.style.overflow = ''"
>
  {homeInitialMinimal && (
    <div
      class="announcement bg-brand-dark-blue"
      :class="pastMarquee ? 'hidden' : ''"
      :style="`max-height: ${44 - announcementProgress}px; opacity: ${1 - (announcementProgress / 44)}; transform: translateY(${-announcementProgress}px);`"
    >
      <a href="/shop" class="text-white">One-of-a-kind pieces, never restocked. →</a>
    </div>
  )}

  <nav class="nav-bar" aria-label="Main navigation" :style="`height: ${72 - (navProgress * 16)}px;`">
    <a href="/" class="nav-logo" aria-label="Rosario Leonardi, Home">
      <img src={rosarioIconGold.src} alt="" aria-hidden="true" class="nav-logo-img" />
      <span class="nav-wordmark">Rosario Leonardi</span>
    </a>

    <div class="nav-right">
      <ul
        class="nav-links-right"
        role="list"
        :style="homeInitialMinimal ? 'max-width: ' + (navProgress * 280) + 'px; opacity: ' + navProgress + '; gap: ' + (navProgress * 1.75) + 'rem;' : ''"
      >
        {navLinks.map(([label, href]) => (
          <li class={label === 'Shop' ? 'nav-item nav-item--shop' : 'nav-item'}>
            <a href={href} class="nav-link">{label}</a>
            {label === 'Shop' && (
              <div class="shop-dropdown" aria-label="Shop categories">
                {shopDropdownLinks.map(([dropdownLabel, dropdownHref]) => (
                  <a href={dropdownHref}>{dropdownLabel}</a>
                ))}
              </div>
            )}
          </li>
        ))}
      </ul>

      <span
        class="nav-group-sep"
        aria-hidden="true"
        :style="homeInitialMinimal ? 'max-width: ' + navProgress + 'px; opacity: ' + navProgress + ';' : ''"
      ></span>

      <div class="nav-icons">
        <button
          type="button"
          class="nav-icon"
          aria-label="Search"
          @click="$store.search.isOpen ? $store.search.close() : $store.search.open()"
        >
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5" aria-hidden="true">
            <circle cx="11" cy="11" r="8" />
            <path stroke-linecap="round" d="m21 21-4.35-4.35" />
          </svg>
        </button>
        <a href={accountHref} class="nav-icon" aria-label="Account">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
            <circle cx="12" cy="7" r="4" />
          </svg>
        </a>
        <button
          type="button"
          class="nav-icon"
          aria-label="Cart"
          @click="$store.cart.open()"
        >
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z" />
            <line x1="3" y1="6" x2="21" y2="6" />
            <path stroke-linecap="round" d="M16 10a4 4 0 0 1-8 0" />
          </svg>
          <span x-show="$store.cart.totalQuantity > 0" x-text="$store.cart.totalQuantity" class="cart-badge"></span>
        </button>
        <span class="nav-divider" aria-hidden="true"></span>
      </div>

      <a href="/shop" class="btn-primary nav-cta">Shop Now</a>

      <button
        type="button"
        class="hamburger"
        :class="open ? 'open' : ''"
        aria-label="Open menu"
        :aria-expanded="open ? 'true' : 'false'"
        @click="open = !open; document.body.style.overflow = open ? 'hidden' : ''"
      >
        <span></span>
        <span></span>
      </button>
    </div>
  </nav>

  <div
    class="mobile-backdrop"
    :class="open ? 'open' : ''"
    :aria-hidden="open ? 'false' : 'true'"
    @click="open = false; document.body.style.overflow = ''"
  ></div>

  <nav
    id="mobile-drawer"
    aria-label="Mobile navigation"
    :aria-hidden="open ? 'false' : 'true'"
    :class="open ? 'open' : ''"
    @click.outside="if (open && !$event.target.closest('.hamburger')) { open = false; document.body.style.overflow = ''; }"
  >
    <div class="mobile-drawer-head">
      <a href="/" class="mobile-drawer-brand" aria-label="Rosario Leonardi, Home" @click="open = false; document.body.style.overflow = ''">
        <img src={rosarioIconGold.src} alt="" aria-hidden="true" />
      </a>
      <button
        type="button"
        class="mobile-close"
        aria-label="Close menu"
        @click="open = false; document.body.style.overflow = ''"
      >
        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12" />
        </svg>
      </button>
    </div>
    {navLinks.map(([label, href]) => (
      <a href={href} class="mobile-link" @click="open = false; document.body.style.overflow = ''">{label}</a>
    ))}
    <a href={accountHref} class="mobile-link" @click="open = false; document.body.style.overflow = ''">Account</a>
    <a href="tel:+15042755222" class="mobile-phone" @click="open = false; document.body.style.overflow = ''">
      Call 504-275-5222
    </a>
  </nav>
</header>

<style>
  .site-nav {
    position: fixed;
    inset: 0 0 auto 0;
    z-index: 100;
    transition: background 0.45s cubic-bezier(0.16,1,0.3,1), backdrop-filter 0.45s cubic-bezier(0.16,1,0.3,1), border-color 0.45s ease;
  }
  .site-nav.scrolled,
  .site-nav:not(.home-initial-minimal) {
    background: oklch(from var(--cobalt) l c h / 0.96);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--border);
    box-shadow: 0 8px 16px rgba(0,0,0,0.08), 0 24px 48px rgba(0,0,0,0.10);
  }
  .announcement {
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    height: 44px;
    padding: 0 24px;
    border-bottom: 1px solid var(--border);
    overflow: hidden;
    max-height: 44px;
    opacity: 1;
    transform: translateY(0);
    will-change: max-height, opacity, transform;
  }
  .announcement.hidden {
    border-bottom-color: transparent;
  }
  .announcement a {
    font-family: var(--font-label);
    font-size: 10px;
    line-height: 1;
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
  }
  .nav-bar {
    max-width: 1440px;
    margin: 0 auto;
    padding: 0 2.5rem;
    display: flex;
    align-items: center;
    height: 72px;
    transition: height 0.45s cubic-bezier(0.16,1,0.3,1);
    will-change: height;
  }
  .nav-logo {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    flex-shrink: 0;
    z-index: 1;
  }
  .nav-logo-img {
    width: 28px;
    height: 34px;
    object-fit: contain;
    flex-shrink: 0;
    transform: translateY(2px);
  }
  .nav-wordmark {
    font-family: var(--font-wordmark);
    font-size: 28px;
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    color: var(--fg);
    line-height: 1;
  }
  .nav-right {
    margin-left: auto;
    display: flex;
    align-items: center;
    gap: var(--space-5);
    z-index: 1;
    transition: gap 0.45s cubic-bezier(0.16,1,0.3,1);
  }
  .nav-links-right {
    display: flex;
    align-items: center;
    gap: 1.75rem;
    list-style: none;
    max-width: 280px;
    opacity: 1;
    overflow: visible;
    visibility: visible;
  }
  .nav-item {
    position: relative;
  }
  .nav-item--shop::after {
    content: '';
    position: absolute;
    top: 100%;
    left: -28px;
    width: calc(100% + 56px);
    height: 24px;
  }
  .nav-link {
    font-family: var(--font-label);
    font-size: 10px;
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
    color: var(--fg-sub);
    position: relative;
    padding-bottom: 2px;
    transition: color 0.2s;
  }
  .nav-link:hover { color: var(--fg); }
  .nav-link::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--accent);
    transition: width 0.3s cubic-bezier(0.16,1,0.3,1);
  }
  .nav-link:hover::after { width: 100%; }
  .shop-dropdown {
    position: absolute;
    top: calc(100% + 10px);
    left: 50%;
    min-width: 160px;
    padding: var(--space-2);
    background: var(--cobalt);
    border-radius: var(--radius-sharp);
    opacity: 0;
    pointer-events: none;
    transform: translate(-50%, -6px);
    transition: opacity 0.18s ease, transform 0.18s ease;
  }
  .shop-dropdown::before {
    content: '';
    position: absolute;
    inset: -10px 0 auto;
    height: 10px;
  }
  .shop-dropdown a {
    display: block;
    padding: 0.65rem 0.75rem;
    color: var(--fg-sub);
    font-family: var(--font-label);
    font-size: 10px;
    letter-spacing: var(--tracking-wider);
    text-align: center;
    text-transform: uppercase;
    transition: color 0.2s, background 0.2s;
  }
  .shop-dropdown a:hover {
    background: oklch(100% 0 0 / 0.05);
    color: var(--fg);
  }
  .nav-item--shop:hover .shop-dropdown,
  .nav-item--shop:focus-within .shop-dropdown {
    opacity: 1;
    pointer-events: auto;
    transform: translate(-50%, 0);
  }
  .home-initial-minimal:not(.scrolled) .nav-links-right,
  .home-initial-minimal:not(.scrolled) .nav-group-sep {
    width: 0;
    max-width: 0;
    gap: 0;
    opacity: 0;
    overflow: hidden;
    pointer-events: none;
    visibility: hidden;
  }
  .home-initial-minimal .nav-links-right {
    transition:
      opacity 0.4s cubic-bezier(0.16,1,0.3,1),
      max-width 0.48s cubic-bezier(0.16,1,0.3,1),
      gap 0.48s cubic-bezier(0.16,1,0.3,1),
      visibility 0.4s ease;
  }
  .home-initial-minimal .nav-group-sep {
    max-width: 1px;
    transition:
      opacity 0.4s cubic-bezier(0.16,1,0.3,1),
      max-width 0.48s cubic-bezier(0.16,1,0.3,1),
      visibility 0.4s ease;
  }
  .nav-group-sep,
  .nav-divider {
    width: 1px;
    height: 18px;
    background: oklch(100% 0 0 / 0.18);
    flex-shrink: 0;
  }
  .nav-icons {
    display: flex;
    align-items: center;
    gap: 1.25rem;
  }
  .nav-icon {
    display: flex;
    align-items: center;
    color: var(--fg-sub);
    transition: color 0.25s ease, transform 0.45s cubic-bezier(0.16,1,0.3,1);
    position: relative;
  }
  .nav-icon:hover { color: var(--fg); }
  .nav-icon svg { width: 17px; height: 17px; }
  .cart-badge {
    position: absolute;
    top: -7px;
    right: -7px;
    width: 16px;
    height: 16px;
    border-radius: var(--radius-pill);
    background: var(--accent);
    color: var(--bg);
    font-family: var(--font-label);
    font-size: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 1;
  }
  .nav-cta {
    padding: var(--space-3) 1.5rem;
    transition:
      background 0.35s cubic-bezier(0.16,1,0.3,1),
      border-color 0.35s cubic-bezier(0.16,1,0.3,1),
      color 0.35s cubic-bezier(0.16,1,0.3,1);
  }
  .site-nav:not(.scrolled) .nav-cta {
    background: oklch(100% 0 0 / 0.08);
    color: var(--fg);
    border: 1px solid oklch(100% 0 0 / 0.22);
  }
  .home-initial-minimal:not(.scrolled) .nav-cta {
    background: var(--accent);
    border-color: var(--accent);
    color: var(--bg);
  }
  .site-nav:not(.scrolled) .nav-cta:hover {
    background: var(--accent);
    border-color: var(--accent);
    color: var(--bg);
  }
  .hamburger {
    display: none;
    flex-direction: column;
    gap: 5px;
    padding: 4px;
  }
  .hamburger span {
    display: block;
    height: 1px;
    background: var(--fg);
    transition: transform 0.25s, width 0.25s;
  }
  .hamburger span:nth-child(1) { width: 22px; }
  .hamburger span:nth-child(2) { width: 14px; align-self: flex-end; }
  .hamburger.open span:nth-child(1) { transform: rotate(45deg) translate(4px, 4px); width: 20px; }
  .hamburger.open span:nth-child(2) { transform: rotate(-45deg) translate(3px, -4px); width: 20px; }
  #mobile-drawer {
    position: fixed;
    inset: 0 0 0 auto;
    background: var(--cobalt);
    z-index: 102;
    width: min(86vw, 380px);
    height: 100vh;
    height: 100dvh;
    padding: 1.25rem 2rem max(2.5rem, env(safe-area-inset-bottom));
    display: flex;
    flex-direction: column;
    opacity: 0;
    pointer-events: none;
    transform: translateX(100%);
    transition: opacity 0.25s ease, transform 0.25s ease;
    overflow-y: auto;
    -webkit-overflow-scrolling: touch;
    box-shadow: -24px 0 48px rgba(0,0,0,0.28);
  }
  #mobile-drawer.open {
    opacity: 1;
    pointer-events: auto;
    transform: translateX(0);
  }
  .mobile-backdrop {
    position: fixed;
    inset: 0;
    z-index: 101;
    background: oklch(0% 0 0 / 0.45);
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.25s ease;
  }
  .mobile-backdrop.open {
    opacity: 1;
    pointer-events: auto;
  }
  .mobile-drawer-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    min-height: 48px;
    margin-bottom: 2rem;
  }
  .mobile-drawer-brand {
    display: inline-flex;
    align-items: center;
  }
  .mobile-drawer-brand img {
    width: 28px;
    height: 34px;
    object-fit: contain;
  }
  .mobile-close {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    color: var(--fg);
    border: 1px solid oklch(100% 0 0 / 0.18);
    border-radius: var(--radius-sharp);
    transition: border-color 0.2s ease, color 0.2s ease;
  }
  .mobile-close:hover {
    border-color: var(--accent);
    color: var(--accent);
  }
  .mobile-close svg {
    width: 18px;
    height: 18px;
  }
  .mobile-link {
    display: block;
    padding: 1.25rem 0;
    font-family: var(--font-label);
    font-size: 11px;
    letter-spacing: var(--tracking-widest);
    text-transform: uppercase;
    color: var(--fg-sub);
    border-bottom: 1px solid var(--border);
  }
  .mobile-phone {
    margin-top: var(--space-4);
    display: none;
    padding: 12px;
    border: 1px solid var(--border-lift);
    color: var(--accent);
    font-family: var(--font-label);
    font-size: 10px;
    letter-spacing: var(--tracking-wider);
    text-align: center;
    text-transform: uppercase;
  }
  @media (max-width: 1023px) {
    .nav-wordmark,
    .nav-links-right,
    .nav-group-sep,
    .nav-divider,
    .nav-cta { display: none; }
    .hamburger { display: flex; }
    .nav-bar { padding: 0 1.5rem; }
    .mobile-phone { display: block; }
  }
</style>
